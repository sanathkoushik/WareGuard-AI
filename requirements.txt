# Core Vision & AI
ultralytics>=8.1.0
# headless build: no GUI (imshow/waitKey) is used anywhere in this repo, and
# headless avoids the libGL.so.1 "ImportError" that opencv-python throws on
# minimal server/container images (Streamlit Cloud, Docker slim, CI).
opencv-python-headless>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
torch>=2.0.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
Pillow>=10.0.0

# Web Dashboard (For Phase 4)
streamlit>=1.30.0

# Assistant (For Phase 5)
requests>=2.31.0
